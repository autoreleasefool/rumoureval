"""Package for classifying tweets by Support, Deny, Query, or Comment (SDQC)."""

def sdqc(tweets):
    """
    Classify tweets into one of four categories - support (s), deny (d), query(q), comment (c).

    :param tweets:
        set of twitter threads
    :type tweets:
        `list` of :class:`Tweet`
    :rtype:
        `dict`
    """
    return {}
